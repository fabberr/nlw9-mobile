/********** Module Imports **********/

// React Native
import { StyleSheet } from 'react-native';

// styles and assets
import { THEME } from '../../theme';

/********** Styles of DuoAdCard Component **********/

export const styles = StyleSheet.create({
  container: {
    width: 180,
    backgroundColor: THEME.COLORS.SHAPE,
    borderRadius: 8,
    padding: 20,
    marginRight: 16,
  }
});
